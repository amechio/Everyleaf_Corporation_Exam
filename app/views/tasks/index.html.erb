<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タスク名</th>
    <th>内容</th>
  </tr>
  <% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.details %></td>
    <td><%= link_to "詳細", task_path(task.id) %></td>
  </tr>
  <% end %>
</table>
<%= link_to "タスクを登録する", new_task_path %>
